# Vol 2 Ch. XIII (9th house) — tuned-chapter re-derivation (NOT held-out)

> **CORRECTION (supersedes the earlier "cross-house held-out" framing of this file).**
> Vol 2 Ch. XIII **is** the tuned `h9` calibration chapter
> (`docs/raman_doctrine/audit/corpora/htjah_h9_calibration.json`, labeled `"ch13
> (Vol. II)"`). The engine was hand-decoded **and** Phase-B-fit on charts 92/93/94, so
> running them through the live engine is **not** independent validation — it was a
> train/test-wall violation to call it "held-out cross-house confirmation." This file is
> retained only as a **tuned-vs-live consistency check** (does the live sign-reconstruction
> engine reproduce the hand-encoded tuned tokens?). The one genuinely held-out strength set
> remains **Ch VII, 4th house** (`REPORT_ch07_4th.md`, 18 rows / 7 charts).

## What this actually is
Charts 92/93/94 reconstructed from Raman's Rāśi+Navāṁśa **sign** diagrams and run through
the live engine, judging the 9th house (Pitrukāraka = Sun). Because these charts are in the
tuned set, the engine's agreement/disagreement here reflects the tuning, not generalization.

## What it still usefully shows (a consistency check, not validation)
- The two engine biases that the **held-out Ch VII** set surfaces also appear on these
  tuned charts (ch94 bhāva under-score; ch93 lord over-credit) — consistent with the
  tuned corpus's own notes (`htjah_h9_calibration.json` grades ch93 lord Venus "weak",
  ch94 bhāva "fairly strong"). This corroborates that the biases are engine-structural,
  but it is **not** independent evidence — the earlier claim that it "confirmed the biases
  on a held-out house" is retracted.
- A.1/A.2 (the natural-benefic bhāva fixes) do **not** move ch94 (its blemisher is the
  natural malefic Sun; its subhakartari is blocked by Ketu in the 8th) — so nothing here
  affected the A.1/A.2 validation, which stands entirely on held-out Ch VII.

## For genuine cross-house held-out
The tuned set is houses 2/7/9/11 + the ch. IV anchor, so the 4th (Ch VII) is clean but the
7th/9th/11th chapters are all tuned. A real cross-house held-out test needs a **non-tuned
house** — the 8th, 10th, or 12th (Vol 2 Ch XII/XIV/XVI). Ch VII exhausted its clean
strength-graded mother-charts, so that is the path if cross-house held-out is wanted.

## Timing note
Charts 92/93 also appear in the daśā **timing** corpus (`REPORT_timing.md`). That is fine:
the tuned h9 corpus is strength-only tokens with no daśā content, and the engine's
Vimśottarī period arithmetic was never fit to any chart — so timing validation on these
charts is untainted by the strength tuning.
