fastapi
uvicorn
pyswisseph
pydantic
pydantic-settings
sqlalchemy>=2.0
asyncpg
aiosqlite
pyjwt
authlib
slowapi
itsdangerous
# Vendored portal (Flask app under /portal/) — astro-web-portal probability engine
flask>=3.0
a2wsgi>=1.10  # WSGI portal mount; replaces deprecated starlette.middleware.wsgi
skyfield>=1.46
jplephem>=2.19
markdown>=3.5
python-dateutil>=2.8
# Phase 5 (Medini ML Truth Layer) — ETL + rule discovery via XGBoost+SHAP.
# beautifulsoup4 + requests for the Astro-Databank scraper.
# pandas + pyarrow for ML-ready parquet storage.
# scikit-learn for stratified split + cross-validation.
# xgboost + shap for the rule discovery model and interpretability.
# matplotlib for SHAP plot rendering (must use Agg backend on headless CI).
requests>=2.31
beautifulsoup4>=4.12
pandas>=2.1
pyarrow>=14.0
# duckdb backs the Silver/Gold catalog and experiment_loader, and is imported at
# module top level by 6 modules including 2 test modules. It was previously
# absent here, so a clean install could not collect tests/test_experiment_loader.py
# or tests/test_duckdb_catalog.py.
duckdb>=0.10
# timezonefinder resolves birth coordinates to a tz for the lunarastro ETL, and is
# imported at module top level by lunarastro_importer + lunarastro_to_natal. Also
# previously absent, so tests/test_lunarastro_to_natal.py could not be collected.
timezonefinder>=6.0
# PyYAML parses the knowledge-library manifest. app/main.py imports
# knowledge_routes, which imports yaml at module top level, so without this
# EVERY test that builds the FastAPI app errors out (142 of them).
PyYAML>=6.0
scikit-learn>=1.3
xgboost>=2.0
shap>=0.45
matplotlib>=3.8
pytest
pytest-asyncio
pytest-cov
httpx
# Pandit-synthesis pipeline (app/llm/pandit_synthesizer.py) — frontier-model
# Vedic chart reading with RAG-grounded corpus retrieval. Sonnet 4.6 is the
# locked default per CLAUDE.md decision 2026-06-01.
anthropic>=0.40
# Stage D dynamic-deephit deps (see docs/superpowers/specs/2026-05-24-fork-a-stage-d-design.md)
# lifelines <0.30 caps numpy<2 (dragging shap/timezonefinder backwards) and 0.27.7
# breaks on scipy>=1.14 (removed trapz); 0.30 drops the numpy cap, same APIs.
lifelines>=0.30,<0.31
torch>=2.0,<3.0
scikit-survival>=0.22,<0.25
