fastapi
uvicorn
pyswisseph
pydantic
pydantic-settings
sqlalchemy>=2.0
asyncpg
aiosqlite
pyjwt
authlib
slowapi
itsdangerous
# Vendored portal (Flask app under /portal/) — astro-web-portal probability engine
flask>=3.0
skyfield>=1.46
jplephem>=2.19
markdown>=3.5
python-dateutil>=2.8
# Phase 5 (Medini ML Truth Layer) — ETL + rule discovery via XGBoost+SHAP.
# beautifulsoup4 + requests for the Astro-Databank scraper.
# pandas + pyarrow for ML-ready parquet storage.
# scikit-learn for stratified split + cross-validation.
# xgboost + shap for the rule discovery model and interpretability.
# matplotlib for SHAP plot rendering (must use Agg backend on headless CI).
requests>=2.31
beautifulsoup4>=4.12
# Offline geocoding for astrocrm_geocode_importer (place name -> lat/lon -> tz).
# Lazily imported, so the app/tests run without them; needed only to run that importer.
geonamescache>=2.0
timezonefinder>=6.0
pandas>=2.0
pyarrow>=14.0
scikit-learn>=1.3
xgboost>=2.0
shap>=0.45
matplotlib>=3.8
pytest
pytest-asyncio
pytest-cov
httpx
# Stage D dynamic-deephit deps (see docs/superpowers/specs/2026-05-24-fork-a-stage-d-design.md)
lifelines>=0.27,<0.30
torch>=2.0,<3.0
scikit-survival>=0.22,<0.25
