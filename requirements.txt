fastapi
uvicorn
pyswisseph
pydantic
pydantic-settings
sqlalchemy>=2.0
asyncpg
aiosqlite
pyjwt
authlib
slowapi
itsdangerous
python-multipart
# Vendored portal (Flask app under /portal/) — astro-web-portal probability engine
flask>=3.0
skyfield>=1.46
jplephem>=2.19
markdown>=3.5
python-dateutil>=2.8
# Phase 5 (Medini ML Truth Layer) — ETL + rule discovery via XGBoost+SHAP.
# beautifulsoup4 + requests for the Astro-Databank scraper.
# pandas + pyarrow for ML-ready parquet storage.
# scikit-learn for stratified split + cross-validation.
# xgboost + shap for the rule discovery model and interpretability.
# matplotlib for SHAP plot rendering (must use Agg backend on headless CI).
requests>=2.31
beautifulsoup4>=4.12
pandas>=2.0
pyarrow>=14.0
scikit-learn>=1.3
xgboost>=2.0
shap>=0.45
matplotlib>=3.8
pytest
pytest-asyncio
pytest-cov
hypothesis
httpx
# NOTE: the heavy Stage-D research deps (torch, lifelines, scikit-survival,
# duckdb) are intentionally NOT here — they fail to build wheels on the CI
# runner (autograd-gamma) and balloon install time. They live in
# requirements-stage-d.txt and are installed only when running the Stage-D /
# survival-analysis / DuckDB-catalog suites locally. The tests that need them
# skip cleanly (pytest.importorskip) when the deps are absent, so the core CI
# suite stays installable and green.
