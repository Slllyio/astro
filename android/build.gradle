// Root build file. Plugin versions are resolved from settings.gradle's
// pluginManagement repositories. AGP 8.5.x requires Gradle 8.7+.
plugins {
    id 'com.android.application' version '8.5.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.24' apply false
}
