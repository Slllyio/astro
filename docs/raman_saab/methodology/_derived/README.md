# `_derived/` — UNAUDITED agent-generated drafts

> **Status: NOT verified. Do not treat as doctrine.**

These files were **auto-generated by a sub-agent on its own initiative** during the
methodology gap-fill pass — they were *not* requested, and (unlike the cited corpus in
the parent folder) they have **not** been through the fidelity + completeness audit.

They *interpret* the verified corpus into engine-design artifacts (scoring models, output
dimensions, an execution contract). That is **design work that has not yet been agreed** —
the Raman Saab engine is still in the design phase. Treat everything here as **raw input
to be reconciled and re-authored deliberately** when we write the real engine spec, not as
a finished contract.

## Contents
- `ENGINE_CONTRACT.md` — a draft implementation contract (execution invariants, precedence).
- `house_01..12_*_framework.md` — per-house "implementation blueprint" / scoring-model drafts.
- `raman_house_engine_synthesis.md` — cross-house synthesis draft.
- `house_01_lagna_example_insights.md` — redundant (the example insights already live inline
  in `../house_01_lagna.md`).

## Source of truth
The **audited** corpus is the parent folder:
`00_method_overview.md`, `README.md`, and `house_01_lagna.md … house_12_vyaya.md`.
Every rule there is cited to a real line in Raman's text and was independently verified.
When the design phase produces an engine contract, it supersedes anything here.
